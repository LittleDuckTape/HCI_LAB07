// Print all odd numbers between 10 and 40. Write two solutions: one with a while loop and one with  a for loop

// While loop:

let num = 11;
while (num <= 40) {
    console.log(num)
    num += 2;
}

// For loop:

for (let i = 11; i <= 40; i += 2) {
    console.log(i);
}